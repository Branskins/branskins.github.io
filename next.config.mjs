/** @type {import('next').NextConfig} */
const nextConfig = {
  // branskins.github.io is a user-Pages site (deployed at the domain root,
  // not a project subpath), so no basePath/assetPrefix is needed.
  output: "export",
};

export default nextConfig;
